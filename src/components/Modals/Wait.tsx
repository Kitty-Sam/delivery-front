import React from 'react';

import { Container, TextTitle } from '~components/Modals/style';

export const Wait = () => (
    <Container>
        <TextTitle>Wait a minute, please</TextTitle>
    </Container>
);
